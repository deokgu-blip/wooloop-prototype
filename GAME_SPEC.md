# 울루프 (Wooloop) — 게임 사양서

> 작성일: 2026-05-15  
> 출처: Google Play `io.addev.testpuzzle001` 영상 분석 + 사용자 클라리피케이션  
> 목적: 자사 클론 개발용. 영상의 메커니즘을 1:1 재현하는 것이 목표.

---

## 1. 게임 정체성

- **장르**: 십자수/자수 풀기(unraveling) 퍼즐
- **테마**: 캔버스에 박힌 컬러 픽셀("wool")을 보빈에 감아 비우는 작업
- **시작 상태**: 픽셀아트(예: 닭)가 자수틀 캔버스를 가득 채움
- **종료 상태**: 캔버스가 완전히 비워짐
- **시점**: 탑다운 2D
- **타깃**: 모바일 세로(Portrait), 단일 HTML, GameReels 플랫폼

---

## 2. 자원

### 2.1 틀 (Carrier) — 보빈을 태우는 메탈 화살표 운반체
- **총 5개**. 캔버스 좌하단의 "더미(stack)"에 보관.
- 카운터 `X/5` 표기: X = 현재 더미에 남은 사용 가능한 틀 수.
- 한 바퀴마다 회수되어 재사용.

### 2.2 보빈 (Bobbin) — 색상 + 카운트를 가진 단추형 자원
- 색상별 종류: 파랑/빨강/흰색/검정/주황/노랑/초록 등 (그림에 따라 가변).
- 카운트: 정수, 보빈 1개 최대 **40**.
- **유한 자원**: 모든 보빈 카운트 총합 = 그림의 wool 픽셀 총합과 **정확히 일치**.
  - 예시: 캔버스에 초록 픽셀이 500개라면 → 보빈 [초록 40 × 12 + 초록 20 × 1] 같은 식으로 합 500.

### 2.3 슬롯 (Storage Slot) — 작업 미완료 보빈 보관소
- 캔버스 바로 아래에 **5칸**.
- 한 바퀴 완주 후 카운트가 남은 보빈이 들어옴.

### 2.4 하단 큐 — 시작 보빈 정렬
- 화면 하단에 보빈이 정렬되어 사용 대기.
- 정렬 형태는 가변 (영상은 3×2 그리드).

---

## 3. 인터랙션 흐름

1. 사용자가 **하단 큐의 보빈** OR **슬롯의 보빈**을 클릭
2. 더미에서 틀 1개가 빠져나와 **7시 방향** (캔버스 좌하단 코너 근처)에서 레일 위로 올라옴 → `X/5` 감소
3. 보빈이 그 틀 위에 탑승하는 연출
4. 틀은 **시계방향**으로 레일을 따라 한 바퀴 회전 시작
5. 회전 종료 후 (자세한 분기는 §6 참조)
6. 사용자는 회전 중에도 다른 보빈을 클릭 가능. 단:
   - **약간의 딜레이** 후에만 추가 클릭 가능 (연속 클릭 방지)
   - 한 바퀴를 도는 모든 보빈은 ">" 화살표 1칸 정도 **간격**을 유지하여 배치됨

---

## 4. 회전 / 이동

- **방향**: 시계방향 (자수틀 안쪽에 새겨진 ">" 화살표가 방향 표시)
- **속도**: 한 바퀴 = **6.5초**, 보빈 색/종류와 무관하게 동일
- **시작점**: 7시 방향 = 캔버스 좌하단 코너 안쪽 (틀 더미가 레일과 만나는 지점)
- **경로 순서** (시계방향, 7시 시작 기준):
  ```
  7시(좌하단) → 좌측 변(위로) → 좌상단 → 상단 변(우로) → 우상단
   → 우측 변(아래로) → 우하단 → 하단 변(좌로) → 7시(시작점 복귀)
  ```
- **보빈 간격**: ">" 1칸 여유 유지. 클릭 딜레이로 강제됨.

---

## 5. Wool 흡수 메커니즘

### 5.1 매칭 조건
- 보빈이 1픽셀(셀) 이동할 때마다, **보빈 중앙축선상**에 같은 색 wool이 있는지 확인.
- **중앙축**의 정의:
  - 보빈이 **좌측 변**에 있을 때 → 보빈의 y좌표를 지나는 **수평선** (캔버스 오른쪽으로 확장)
  - 보빈이 **상단 변**에 있을 때 → 보빈의 x좌표를 지나는 **수직선** (캔버스 아래쪽으로 확장)
  - 보빈이 **우측 변**에 있을 때 → 보빈의 y좌표를 지나는 **수평선** (캔버스 왼쪽으로 확장)
  - 보빈이 **하단 변**에 있을 때 → 보빈의 x좌표를 지나는 **수직선** (캔버스 위쪽으로 확장)

### 5.2 흡수 룰
- 매칭 직선상의 **가장 외곽(보빈에 가장 가까운) 비어있지 않은 wool 1개**를 검사.
  - 그 셀이 **보빈 색과 같으면** → 끌어당김.
  - 그 셀이 **다른 색이면** → 막힘, 흡수 못 함 (직선상 안쪽에 같은 색이 있어도 못 끌어옴).
  - 빈 셀(이미 끌어간 자리)은 건너뜀.
- 흡수 1회당:
  - 보빈 카운트 **−1**
  - 보빈 중앙의 실타래가 1단계 **굵어짐**
- **정확히 같은 색**만 매칭. 유사 색 매칭 없음.
- 보빈은 흡수 중에도 **계속 반시계방향 이동** → 실은 곡선(trailing curve)으로 휘어짐.
- 카운트가 0이 되는 **즉시 그 자리에서** 보빈+틀이 페이드하며 사라짐 (한 바퀴 완주 여부 무관, 더미로 복귀 애니메이션 없음).
- 사라진 직후 그 자리에 **스파클/버블** 연출 (~0.5s).
- 카운트 0 시점에 더미 카운터 **즉시 +1** (페이드 완료를 기다리지 않음).

### 5.3 매칭 픽셀이 없을 때
- 보빈은 이동만 함. 한 바퀴 후 처리는 §6 참조.

---

## 6. 한 바퀴 종료 분기

### 6.1 카운트 = 0 (회전 도중 사라진 경우)
- 보빈+틀이 **그 자리에서 페이드 + 스케일 업** (0.35s)
- 사라진 자리에 **스파클 버블** (~0.55s) — 빛 펄스 + 가로/세로 십자 스파이크
- `X/5` **즉시 +1** (애니메이션과 무관하게 카운터는 바로 갱신)
- 카리어가 더미로 날아가는 별도 애니메이션 **없음**

### 6.2 카운트 > 0 (한 바퀴 완주 후 잔량 있음)
- 보빈은 비어있는 슬롯으로 **점프 이동** → 거기에 거치됨
- 틀은 레일 아래로 들어간 뒤 **더미의 맨 뒤에 쌓임** → `X/5` +1
- 모든 슬롯이 가득 차 있으면 → **게임 오버** (§8 참조)

### 6.3 슬롯 보빈 재출격
- 슬롯에 있는 보빈 클릭 → §3과 동일 흐름 (틀 1개 소비, 7시 시작, 시계방향 회전)
- 슬롯 → 비워짐 → `X/5` 감소

---

## 7. 부스트 모드 (엔드게임)

### 7.1 발동 조건
- **전체 잔여 보빈 수 ≤ 5** 시 발동
- "전체"의 정의: 레일 + 슬롯 + 큐 (보이지 않는 후속 보빈 포함)
- 사용자 시야 기준이 아닌 게임 전체 자원 기준

### 7.2 발동 효과
- 전체 게임 속도 **2배** (6.5초 → 3.25초/바퀴)
- 레일 위 보빈은 **한 바퀴 종료 시 회수되지 않고** 카운트 0이 될 때까지 무한 회전
- 카운트 0이 되면 (부스트 모드에서도) 보빈은 즉시 소멸
- 흡수 룰은 동일 (1픽셀 이동 = 매칭 시 1픽셀 흡수)

---

## 8. 승/패 조건

### 8.1 클리어
- 캔버스의 모든 wool 픽셀이 제거된 시점
- `GameEnd("success")` 송신

### 8.2 게임 오버
- 한 바퀴를 완주한 보빈에 카운트가 남았는데, 빈 슬롯이 없을 때 (5/5 모두 점유)
- 즉, 보빈이 슬롯에 거치되어야 하지만 자리가 없는 상황
- `GameEnd("fail")` 송신

---

## 9. 화면 레이아웃 (위 → 아래)

```
┌─────────────────────────────┐
│ (상단 80px 금지 영역)        │
├─────────────────────────────┤
│  ┌───────────────────────┐  │
│  │ ▼ ▼ ▼ ▼ ▼ ▼ ▼  ← 자수틀 │
│  │ ▷ ┌─────────────┐ ◁  │  │  (메탈 림 + 시계방향 ">" 화살표)
│  │ ▷ │             │ ◁  │  │
│  │ ▷ │   캔버스    │ ◁  │  │  ← wool 픽셀 격자
│  │ ▷ │  (wool 그리드) │ ◁  │  │
│  │ ▷ └─────────────┘ ◁  │  │
│  │ ▲ ▲ ▲ ▲ ▲ ▲ ▲       │  │
│  └───┬───────────────────┘  │
│   더미│ X/5                  │
│  └──┬┘                       │
│     │                        │
│  ┌──┴──┬─────┬─────┬─────┬─────┐
│  │SLOT │SLOT │SLOT │SLOT │SLOT │  ← 5칸 슬롯
│  └─────┴─────┴─────┴─────┴─────┘
│                                  │
│  하단 보빈 큐 (3×2 정렬)         │
│  ◯ ◯ ◯                           │
│  ◯ ◯ ◯                           │
└──────────────────────────────────┘
```

- **자수틀(hoop)**: 둥근 모서리, 메탈 광택, 내측 림에 시계방향 ">" 화살표
- **레일**: 자수틀 내측 림 = 보빈/틀이 이동하는 트랙
- **더미**: 좌하단에 카드처럼 쌓인 4~5개의 틀
- **X/5 카운터**: 더미 옆 작은 라벨

---

## 10. 시각 연출 (Visual Effects)

### 10.1 보빈 상태
- **베이스**: 둥근 단추형 (탑다운 시점)
- **색상 띠**: 가장자리 색상이 보빈 색을 표시
- **중앙 실타래**: 카운트가 줄어들수록 단계적으로 굵어짐 (예: 4단계)
- **이동 중**: 살짝 회전(축 회전) 애니메이션 — "실패가 돈다" 느낌

### 10.2 곡선 실 (Pulling Thread)
- wool 픽셀이 흡수될 때, 픽셀 위치에서 보빈 중앙까지 **베지어 곡선**으로 실 라인 그림
- 보빈이 계속 이동하므로 종점이 갱신되어 자연스러운 trailing curve
- 픽셀이 보빈에 도달하면 사라지고 실타래 굵기 1단계 증가

### 10.3 wool 흡수 펄스
- 흡수 순간 wool 픽셀이 살짝 튀어오름 (scale 1.0 → 1.2 → 보빈 방향 이동)
- 보빈 자체도 미세 펄스
- Light haptic 트리거

### 10.4 한 바퀴 종료 연출
- **카운트 0**: 보빈+틀이 그 자리에서 페이드+스케일 업(0.35s) + 스파클 버블(0.55s). 더미 카운터 즉시 +1.
- **카운트 잔량**: 보빈은 슬롯으로 점프(0.3초 호 운동), 틀은 레일 아래로 슬라이드 → 더미 맨 뒤로 (1초 정도)

### 10.5 한 색 클리어
- 캔버스에서 한 색 픽셀이 0개가 되는 순간 → Medium haptic + 짧은 빛 펄스

### 10.6 클리어
- 전체 wool 제거 시 → 캔버스 위에 폭죽/별 파티클 → Medium haptic → `GameEnd("success")`

### 10.7 자수 매듭 (Wool Texture)
- 픽셀 1개 = X자 매듭 모양 (cross-stitch knot)
- `Graphics`로 작은 X 두 줄 또는 PNG 텍스처

---

## 11. 햅틱 매핑

| 시점 | 강도 |
|---|---|
| wool 1픽셀 흡수 | Light |
| 보빈 슬롯 거치 | Light |
| 한 색 클리어 | Medium |
| 게임 클리어 | Medium |
| 게임 오버 | Medium |

- HapticThrottle 인터벌: light 30ms, medium 200ms (CLAUDE.md 표준).

---

## 12. 기술 스택

- **렌더러**: PixiJS r7 (CDN)
- **물리**: 없음 (Matter.js 불필요)
- **사운드**: Web Audio API (절차적 SFX)
- **언어**: Vanilla JS ES2020+
- **파일**: 단일 HTML, 자산은 추후 PNG 인라인 또는 외부 `assets/` 폴더
- **좌표계**: Y-down, 1u = 1px

---

## 13. 앱 통신

§ CLAUDE.md "앱 ↔ 게임 이벤트 프로토콜" **고정 사양** 그대로 적용:
- 송신: `GameStart` / `success` / `fail` / `LightVibrate` / `MediumVibrate`
- 수신: `pause` / `resume` / `start` / `uitoggle`
- 부팅 race 방지: `OnMessageFromFlutter` buffering stub.

---

## 14. 튜닝 상수 (GameTune)

```js
const GameTune = {
  rail: {
    lapDuration: 6.5,           // 한 바퀴 (초)
    boostMultiplier: 2.0,       // 부스트 시 속도 배율
    bobbinSpacing: 1,           // 보빈 간 최소 간격 (셀 단위)
    clickCooldown: 0.7,         // 연속 클릭 방지 (초)
  },
  carrier: {
    total: 5,                   // 틀 총 개수
    returnSpeedFast: 1200,      // 카운트 0일 때 더미 복귀 속도 (px/s)
    returnSpeedSlow: 600,       // 카운트 잔량일 때 더미 복귀 속도
  },
  slots: {
    total: 5,                   // 슬롯 총 개수
  },
  boost: {
    triggerThreshold: 5,        // 전체 잔여 보빈 ≤ 5 시 발동
  },
  visual: {
    threadStages: 4,            // 실타래 굵기 단계
    pullPulseDuration: 0.15,    // 흡수 펄스 (초)
  },
  haptic: {
    lightInterval: 30,
    mediumInterval: 200,
  },
};
```

---

## 15. 테스트 패턴 (프로토타입용)

8×8 그리드, 60 wool 픽셀 + 4 빈 칸:
```
B B B B B B B B
B R R R R R R B
B R W W W W R B
B R W . . W R B
B R W . . W R B
B R W W W W R B
B R R R R R R B
B B B B B B B B
```
- B = 파랑 (28픽셀)
- R = 빨강 (20픽셀)
- W = 흰색 (12픽셀)
- `.` = 빈 칸 (4픽셀)

총 wool = 60.

**보빈 할당**:
- 파랑: 14 + 14 = 28 (2개)
- 빨강: 10 + 10 = 20 (2개)
- 흰색: 6 + 6 = 12 (2개)

→ **총 6개** 보빈. 합 = 60 픽셀.  
→ 게임 진행 중 잔량이 5 이하로 떨어지는 시점부터 부스트 모드 진입 확인 가능.

---

## 16. 확정되지 않은 항목 (TODO)

- BGM/SFX 사운드 디자인 (자수 슬라이드, 흡수 ASMR, 클리어 팡파레)
- 클릭 딜레이의 정확한 값 (현재 0.7초로 가정)
- 보빈 큐 정렬 형태 (현재 3×2 가정, 화면 크기에 따라 가변 검토)
- 슬롯에 거치된 보빈의 위치 정렬 (left-fill / center-fill 등)
- 카운터 옆 아이콘 디자인 (영상의 "말려있는 도면" 모티프)
